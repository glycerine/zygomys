;; source all the tests
(def tests (nsplit (system `ls -1 tests/*.zy`)))
(for [(def i 0) (< i (len tests)) (def i (+ i 1))]
   (printf "done with %d '%s'\n" i (aget tests i))
   (source (aget tests i)))
